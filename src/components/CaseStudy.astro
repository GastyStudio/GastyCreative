---
import { TrendingUp, UserCheck, Monitor } from 'lucide-astro';

const data = [
  { label: "Crecimiento Orgánico", value: "+300%", icon: TrendingUp },
  { label: "Seguidores Reales", value: "+20.000", icon: UserCheck },
  { label: "Lead Gen Automático", value: "24/7", icon: Monitor },
];
---

<section class="py-24 relative overflow-hidden bg-charcoal-grey">
  <div class="container mx-auto px-6 grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
    
    <div data-aos="fade-right">
      <span class="text-electric-blue font-bold tracking-widest uppercase text-sm block mb-4">Caso de Éxito / Marca Personal</span>
      <h2 class="text-3xl md:text-6xl font-bold mb-8 leading-tight">Del Cero al <span class="text-gradient">Impacto Real</span></h2>
      <p class="text-white/70 text-xl leading-relaxed mb-10">
        Escalé mi propia marca personal aplicando las mismas estrategias técnicas que ofrezco a mis clientes. No solo hablamos de métricas de vanidad; hablamos de autoridad, confianza y conversión real.
      </p>
      
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-6">
        {data.map((item, index) => (
          <div 
            class="p-6 rounded-2xl bg-black-deep/40 border border-white/5 backdrop-blur-md text-center group hover:scale-105 transition-transform"
            data-aos="zoom-in"
            data-aos-delay={index * 150}
          >
            <item.icon class="w-8 h-8 text-electric-blue mx-auto mb-4 opacity-70 group-hover:opacity-100 transition-opacity" />
            <div class="text-3xl font-bold mb-1">{item.value}</div>
            <div class="text-white/40 text-sm font-semibold uppercase tracking-wider">{item.label}</div>
          </div>
        ))}
      </div>
    </div>

    <div class="relative" data-aos="fade-left">
      <!-- "Data Visualization" Mockup -->
      <div class="relative z-10 p-4 sm:p-8 rounded-[40px] bg-gradient-to-br from-white/10 to-white/5 border border-white/10 backdrop-blur-xl shadow-2xl">
        <video 
          src="https://media.giphy.com/media/v1.Y2lkPTc5MGI3NjExM3N2cXBzNHZzNHZzNHZzNHVzNHVzNHVzNHVzNHVzNHVzNHVzNHVzNHYmZXA9djFfaW50ZXJuYWxfZ2lmX2J5X2lkJmN0PWc/3o7TKSjPKYM911n7nS/giphy.mp4" 
          autoplay muted loop playsinline
          class="rounded-3xl w-full h-[300px] object-cover border border-white/10 grayscale-[0.5] group-hover:grayscale-0 transition-all duration-700"
        ></video>
        
        <div class="mt-8 space-y-4">
          <div class="h-4 w-3/4 bg-white/10 rounded-full overflow-hidden relative">
            <div class="absolute inset-0 bg-electric-blue w-[85%] animate-pulse"></div>
          </div>
          <div class="h-4 w-1/2 bg-white/10 rounded-full overflow-hidden relative">
            <div class="absolute inset-0 bg-neon-violet w-[60%] animate-pulse delay-500"></div>
          </div>
        </div>
      </div>
      
      <!-- Decorative elements -->
      <div class="absolute -top-10 -right-10 w-40 h-40 bg-electric-blue/20 blur-[60px] rounded-full"></div>
      <div class="absolute -bottom-10 -left-10 w-40 h-40 bg-neon-violet/20 blur-[60px] rounded-full"></div>
    </div>
  </div>
</section>
